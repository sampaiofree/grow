<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve Laravel from /public when the docroot is not set to it.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
